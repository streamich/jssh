import context = require("./context");
var contextify = require("contextify");


/**
 * TODO: Run in `contextify` context.
 */
export = ContextContextify; class ContextContextify extends context.Context implements context.IContext {



}