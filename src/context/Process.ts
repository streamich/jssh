/// <reference path="../typing.d.ts" />
import context = require("./context");


/**
 * TODO: Run JS in a separate process.
 */
export = ContextProcess; class ContextProcess extends context.Context implements context.IContext {

    start() {}

    run(code) {}

}